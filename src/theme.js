const colors = {
  red: '#fc5235',
  grayLighter: '#e6e8ec',
}

const radii = [0, 2, 4, 8, 16, 24];

const spaces = [0, 2, 4, 8, 16, 24, 32, 40, 48, 56, 64, 72, 80];

const fontSizes = [12, 14, 16, 18, 20, 24];
const fontWeights = [100, 300, 500, 900];
const lineHeights = [1, 1.2, 1.4, '100%'];
export default {
  colors,
  radii,
  fontSizes,
  fontWeights,
  lineHeights,
  spaces,
};
